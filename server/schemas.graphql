scalar Date
type Query {
    products: [Product]
}

type Product {
    id: ID!
    date: Date
    city: String
    name: String
    description: String
}

type Mutation {
    post(title: String!): [Product]!
}
