scalar Date
type Query {
    products(filter: [String]): [Product]
}

type Product {
    id: ID!
    date: Date
    city: String
    name: String
    type: String
    description: String
}

type Mutation {
    post(title: String!): [Product]!
}
