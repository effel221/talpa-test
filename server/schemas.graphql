scalar Date
type Query {
    flights: [Product]
    hotels: [Product]
    car_rentals: [Product]
}

type Product {
    id: ID!
    date: Date
    city: String
    name: String
    type: String
    description: String
}

type Mutation {
    post(title: String!): [Product]!
}
